# Copy to .env for local development. Never commit a real .env file.

PORT=3000
APP_ENV=development
APP_VERSION=local

# Leave DB_HOST unset to run without a database (health check will report "skipped").
DB_HOST=
DB_PORT=5432
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=changeme
DB_SSL=false
